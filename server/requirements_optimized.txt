# Core FastAPI and ASGI server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Async database support
aiopg==1.4.0
psycopg2-binary==2.9.9

# Performance and monitoring
psutil==5.9.6
uvloop==0.19.0
httptools==0.6.1

# Existing dependencies (keep current versions)
python-dotenv
bcrypt
python-jose[cryptography]
websockets
schedule
numpy
pandas
requests
yfinance

# Optional: Redis for caching (if available)
# redis==5.0.1
# aioredis==2.0.1 